<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permiso para usar internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="FinalProject"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProject"
        tools:targetApi="31">

        <!-- Pantalla principal al iniciar la app -->
        <activity
            android:name=".ui.Pantalla_usuario_inicial"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.Login_screen" android:exported="false" />
        <activity android:name=".ui.Signup_screen" android:exported="false" />
        <activity android:name=".ui.Admin_Home_screen" android:exported="false" />
        <activity android:name=".ui.Loading_screen" android:exported="false" />
        <activity android:name=".ui.Pantalla_usuario_info" android:exported="false" />
        <activity android:name=".ui.Pantalla_usuario_reservados" android:exported="false" />
        <activity android:name=".ui.Pantalla_usuario_reservar" android:exported="false" />


    </application>


</manifest>